[project]
name = "test-mcp"
version = "0.1.0"
description = "A simple MCP server that says hello world"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.1.0",
]

[project.scripts]
test-mcp = "test_mcp.server:main"
