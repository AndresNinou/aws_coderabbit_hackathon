# Backend API settings
PROJECT_NAME=FastAPI Backend

# !!! CRITICAL: Change this in production deployments !!!
# Application will refuse to start if ENVIRONMENT != local and this is still default
SECRET_KEY=changethis

API_V1_STR=/api/v1
API_HOST=http://localhost:8000

# !!! IMPORTANT: Must be 'production' or 'staging' in real deployments !!!
# Setting this to 'local' exposes private debug endpoints
ENVIRONMENT=local

# CORS settings
BACKEND_CORS_ORIGINS=["http://localhost:8000", "https://localhost:8000", "http://localhost", "https://localhost"]

# Domain
DOMAIN=localhost

# Docker build
DOCKER_IMAGE_BACKEND=backend

# DATABASE_URL is REQUIRED in all environments - no default fallbacks!
# Format: postgresql://user:password@host:port/dbname
# !!! IMPORTANT: Must be PostgreSQL in all environments - Prisma owns schema !!!
DATABASE_URL="postgresql://postgres:postgres@db:5432/app"

# Sentry configuration (monitoring)
# SENTRY_DSN="https://your-project@your-org.ingest.sentry.io/number"
# Performance monitoring sample rate (0.0-1.0, default: 0.1)
# SENTRY_TRACES_SAMPLE_RATE=0.1

# Uvicorn configuration
# Logging level
LOG_LEVEL=info

# Claude Code SDK configuration
# ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# Claude Database URL
# CLAUDE_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/claude_code_agent"

# GitHub App configuration for PR creation
GITHUB_APP_ID=123456            # your app id (integer)
GITHUB_APP_PRIVATE_KEY_B64=<base64 of your .pem>  # run: base64 -w0 your-key.pem
# optional but recommended defaults for commits the server makes
GIT_DEFAULT_AUTHOR_NAME=Security Agent Bot
GIT_DEFAULT_AUTHOR_EMAIL=bot@example.com